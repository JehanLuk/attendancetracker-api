@page
@model TelaAdministrador
@{
    ViewData["Title"] = "Tela do Administrador";
}
    <link rel="stylesheet" href="~/css/TelaAdministrador.css" />


@* <div class="admin-container">
    <header class="admin-header">
        <h2>Bem-vindo à Tela do Administrador</h2>
    </header>

    <div class="admin-stats">
        <div class="stat-card">
            <h3>Total de Usuários</h3>
            <p class="stat-number">@Model.NumeroDeUsuarios</p>
        </div>
        
        <div class="stat-card">
            <h3>Cadastrados</h3>
            <p class="stat-number">@Model.UsuariosCadastrados</p>
        </div>
        
        <div class="stat-card">
            <h3>Usuários Ativos</h3>
            <p class="stat-number">@Model.UsuariosAtivos</p>
        </div>
    </div>

    <div class="admin-content">
        <div class="content-section">
            <h3>Usuários Recentes</h3>
            <div class="user-list">
                @if (Model.UsuariosRecentes?.Any() == true)
                {
                    <ul>
                        @foreach (var usuario in Model.UsuariosRecentes)
                        {
                            <li>
                                <span class="user-name">@usuario.Nome</span>
                                <span class="user-date">@usuario.DataDeCadastro.ToShortDateString()</span>
                            </li>
                        }
                    </ul>
                }
                else
                {
                    <p class="no-data">Nenhum usuário recente</p>
                }
            </div>
        </div>

        <div class="content-section">
            <h3>Relatórios de Atividade</h3>
            <div class="reports-list">
                @if (Model.RelatoriosDeAtividade?.Any() == true)
                {
                    <ul>
                        @foreach (var relatorio in Model.RelatoriosDeAtividade)
                        {
                            <li>
                                <span class="report-title">@relatorio.Titulo</span>
                                <span class="report-date">@relatorio.Data.ToShortDateString()</span>
                            </li>
                        }
                    </ul>
                }
                else
                {
                    <p class="no-data">Nenhum relatório disponível</p>
                }
            </div>
        </div>
    </div>

    <div class="admin-actions">
        <h3>Ações Administrativas</h3>
        <div class="action-buttons">
            <a asp-page="/GerenciarUsuarios" class="btn btn-primary">Gerenciar Usuários</a>
            <a asp-page="/Relatorios" class="btn btn-secondary">Ver Relatórios Detalhados</a>
        </div>
    </div>

    <div class="search-section">
        <h3>Buscar Usuários</h3>
        <form method="get" class="search-form">
            <input type="text" name="searchTerm" placeholder="Buscar usuários..." />
            <button type="submit" class="btn btn-search">Buscar</button>
        </form>
        
        @if (Model.ResultadosDaBusca?.Any() == true)
        {
            <div class="search-results">
                <h4>Resultados da Busca</h4>
                <ul>
                    @foreach (var usuario in Model.ResultadosDaBusca)
                    {
                        <li>
                            <span class="user-name">@usuario.Nome</span>
                            <span class="user-date">@usuario.DataDeCadastro.ToShortDateString()</span>
                        </li>
                    }
                </ul>
            </div>
        }
    </div>

    <div class="system-monitoring">
        <h3>Monitoramento do Sistema</h3>
        <div class="grafico-uso">
            <!-- Espaço para gráfico de uso do sistema -->
            <p>Gráfico de uso do sistema será implementado aqui</p>
        </div>
    </div>

    <div class="admin-messages">
        <h3>Mensagens Administrativas</h3>
        <div class="mensagens-container">
            <!-- Caixa de mensagens administrativas -->
            <p>Sistema de mensagens será implementado aqui</p>
        </div>
    </div>
</div> *@
<script src="~/js/infoAluno.js"></script>